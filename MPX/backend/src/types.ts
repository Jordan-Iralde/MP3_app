/**
 * Global type definitions for the Resonix backend.
 */

declare global {
  namespace Express {
    interface Request {
      // Add custom request properties if needed in future
    }
  }
}

export {};
